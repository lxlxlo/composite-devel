#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/cmake.mk

DEB_CMAKE_EXTRA_FLAGS = -DWANT_LRDF=ON
DEB_DESTDIR = $(CURDIR)/debian/composite

#binary/composite::
#	cd $(DEB_BUILDDIR) && ctest
