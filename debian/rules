#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/cmake.mk

DEB_CMAKE_EXTRA_FLAGS = -DWANT_LRDF=ON
DEB_BUILDDIR = ./debian/build

#binary/composite::
#	cd $(DEB_BUILDDIR) && ctest
